query GetGroupsByUserId {
  getGroupsByUserId {
    id
    admin {
      id
      username
      nickname
      profileImageUrl
    }
    members {
      username
      nickname
      profileImageUrl
    }
    links {
      user {
        id
        username
        nickname
      }
      url
    }
    name
    description
  }
}
