query GetGroup($getGroupId: ID!) {
  getGroup(id: $getGroupId) {
    id
    name
    description
    admin {
      id
      username
      nickname
      profileImageUrl
    }
    members {
      id
      username
      nickname
      profileImageUrl
    }
    links {
      id
      user {
        id
        nickname
        username
      }
      url
    }
  }
}
